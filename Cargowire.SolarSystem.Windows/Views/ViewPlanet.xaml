<UserControl x:Class="Cargowire.SolarSystem.Windows.Views.ViewPlanet"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <TextBlock x:Name="PageTitle" Text="{Binding Name}" Margin="9,-7,0,0"/>
            <Image Source="{Binding Image}" Stretch="None"/>
            <TextBlock Text="{Binding Path=Mass, StringFormat='Mass: {0} kg'}" Margin="9,-7,0,0" />
            <TextBlock Text="{Binding Path=Diameter, StringFormat='Diameter: {0} km'}" Margin="9,-7,0,0" />
            <TextBlock Text="{Binding Path=DistanceFromSun, StringFormat='Distance from the Sun: {0} km'}" Margin="9,-7,0,0"/>
            <TextBlock x:Name="PageDescription" Text="{Binding Description}" Margin="9,10,0,0" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</UserControl>
